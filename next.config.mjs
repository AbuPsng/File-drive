/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        remotePatterns: [
            {
                hostname: 'giddy-otter-661.convex.cloud',
            },
        ],
    },
};

export default nextConfig;
